%tokens

token1 token2 token3 token4 

%rules

init
	: << token4 >> 
	| << token1 || token4 >> 
	| << token2 || token4 >> 
	| << token1 || token2 || token4 >> 
	| << token3 || token4 >> 
	| << token1 || token3 || token4 >> 
	| << token2 || token3 || token4 >> 
	| << token1 || token2 || token3 || token4 >> 
	;
